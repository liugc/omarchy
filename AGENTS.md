# omarchy 笔记仓库

个人笔记仓库：记录 omarchy（Arch + Hyprland）的技巧与问题修复。仓库根目录下每个 `*.md` 就是一篇笔记。

## 规则

- 用户说「记录」「记一下」「记下来」「帮我记」「保存这个」「写个笔记」「这个存一下」时，**必须**走
  `skill://omarchy-notes` 的流程落盘，不要只在对话里复述一遍就结束。
- 一篇笔记一个主题，文件名即 slug；同一主题再次记录时更新原文件，不新建重复副本。
- 笔记内容的唯一来源是本次会话中真实执行过、验证过的命令与输出；没验证的东西要标注「未验证」。

Skill 本体在 `.agents/skills/omarchy-notes/`，同时软链到 `~/.agents/skills/omarchy-notes`，
因此在别的目录开会话也能触发。
